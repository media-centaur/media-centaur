/**
 * Watchlist page behavior.
 *
 * Layout: a single grid of watchlist cards. Left at the grid's left edge
 * returns to the sidebar (framework-handled via the `watchlist` zone
 * layout). No page-specific actions — the framework defaults cover it.
 */
export function createWatchlistBehavior() {
  return {}
}
