/**
 * Discovery page behavior.
 *
 * Layout: the zone-tabs strip above one body zone — the row grid on
 * Recommendations and the watchlist, the `people` shelf of person cards
 * on Friends. Left at the body's left edge returns to the sidebar
 * (framework-handled via the `discovery` zone layout). No page-specific
 * actions — the framework defaults cover it.
 */
export function createDiscoveryBehavior() {
  return {}
}
