/**
 * Discovery page behavior.
 *
 * Layout: the zone-tabs strip above a grid of watchlist rows. Left at the
 * grid's left edge returns to the sidebar (framework-handled via the
 * `discovery` zone layout). No page-specific actions — the framework
 * defaults cover it.
 */
export function createDiscoveryBehavior() {
  return {}
}
